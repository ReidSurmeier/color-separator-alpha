[pytest]
markers =
    gpu: tests requiring GPU or model weights (skipped in CI and when CUDA is absent)

# Default per-test timeout (seconds).  Prevents GPU-init hangs from blocking CI.
timeout = 30
